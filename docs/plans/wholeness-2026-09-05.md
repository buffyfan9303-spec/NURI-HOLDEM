# 전체 완성도 개선 추적 — 2026-09-05 (Codex 실행명령문 + 모바일 점검 27건)

> 단일 추적 문서. 상태 어휘: **이미 해결 / 코드 확인 / 실행 검증 / 미검증 / 결정 필요 / 승인 대기**.
> 작업 브랜치 `wholeness/2026-09-05`(main 32bcf3f 기반). **운영 배포·DB 마이그레이션·푸시는 오너 승인 전까지 하지 않는다.**

## 0. 현재 상태(세션 끊김 대비)
- 브랜치: `wholeness/2026-09-05` — 모바일 점검 27건(7 묶음) 병합 완료, E2E 쓰기 차단 가드 커밋(da97aab).
- 게이트: `npx tsc -b` 0 · `npx eslint .` 0 error(26 warning 기존) · `npx vitest run` 508/508 · Playwright: 실행 중/결과는 §4.
- **이미 운영에 반영된 것(지시 이전)**: 마이그레이션 `20260905o_table_privilege_hygiene`(anon·authenticated 의 TRUNCATE/REFERENCES/TRIGGER 561건 회수, DML 무변경). 롤백: 파일 상단 주석.
- **승인 대기(운영 미적용)**: 마이그레이션 `20260905j`(바인 요청 영업일 RPC) · `k`(출석 적용 공유 함수 + check_in jsonb) · `l`(방문=체크인) · `m`(숨김 글 RLS) · `n`(조회수 dedupe). 적용 순서: j·l·m·n → 클라 배포 → k(check_in 반환 타입 변경, 클라가 문자열/객체 둘 다 처리).
- 테스트 대상 환경: 프런트 `.env.local` 과 E2E 모두 **운영 프로젝트(idsxiqspecrucvfvtgbw)** 를 가리킨다. 격리 프로젝트 없음 → E2E 는 `e2e/_fixtures.ts` 가드로 **읽기·STABLE RPC 만 통과**(쓰기는 네트워크 단에서 차단, 노드 `restAs` 도 차단). 로그인 스펙(33건)은 자격증명 없이 skip = **미검증**.
- 다른 작업자 흔적 보존: `.agents/`, `.codex/`, `AGENTS.md` 미추적 유지. `public/sitemap.xml` 은 빌드가 lastmod 를 갱신하는 생성물.

## 1. 모바일 점검 27건(2026-09-05 오전 워크플로 확정) — 구현 상태
| 묶음 | 항목 | 상태 | 증거 |
|---|---|---|---|
| A+B 장부·출석 | #1 자정 이후 바인 요청 소실 · #2 이용권 사용→출석 4시간 봉쇄 · #22 두 번째 체크인 '+3점' 오안내 · #23 체크인 후 점수 미반영 · #8(c) | 코드 확인(클라) / **승인 대기(j·k)** | f7ab692 · vitest checkins.result.test |
| C 랭킹 허브 | #3 인증 제출 무음 · #15 탭 44px · #17 스켈레톤 CLS · #19 잔액 미확정 · #20 실패 위장 · #21 내 행 강조 · 국내 순위 금액 제거 | 코드 확인 | 3d1f0ef · community.balance.test |
| D 프로필 | #4 아바타 제거 미반영 · #5 재업로드 캐시 · #13 OTP 탭 튕김 | 코드 확인 | 40c5780 · auth.profilePatch.test |
| E+F 대시보드 | #8 방문=체크인 · #6 바인/머니인 단위 · #7 도감 backstack · #14 · #18 입력 보존 · #26 내 글 수 · #27 44px | 코드 확인 / **승인 대기(l)** | 1f9c859 · checkins.visits.test |
| G 이용권 | #7 RedeemSheet backstack · #9 보유 장수 · #14 · #27 | 코드 확인 | 6ea8aad · vouchers.held.test |
| H 게시판 | #10 광고→고정→HOT · #15 서브탭 44px · #16 · #24 · #25 | 코드 확인 | 1a043ae · pinnedFirst.test |
| I+J+K 서버·시트 | #11 숨김 글 RLS · #12 조회수 dedupe · #14 Modal safe-area | 코드 확인(클라) / **승인 대기(m·n)** | 07766a4 · community.hidden.test |

## 2. Codex 실행명령문 F01~F11 — 감사 분류(2026-09-05, main 32bcf3f 대조)

감사 워크플로 wf_0b8d3585 결과. **적대적 검증 에이전트는 사용량 한도로 전부 실패** → 결함 30건은 '코드 확인(미검증 보고)' 이며, 구현자가 각자 재확인한 뒤 고친다. 업무 경로 R1~R8 감사는 같은 이유로 미실행 = **미검증**.

| 항목 | 판정 | 결함 | 처리 |
|---|---|---|---|
| F01 포스터→사이드 장부 게임 번호 유실 | 코드 확인 | 2 | 배선 수정 완료(1b5e674) · 새 장부 착지 문제는 W1 |
| F02 게임별 성적 완료 여부가 날짜로 뭉침 | 코드 확인 | 4 | W1 |
| F03 예약·방문·참가 의미와 날짜 | 코드 확인 | 2 | W6 (노쇼 확정 표기는 결정 필요) |
| F04 CRM 금액이 장부와 다른 산식 | 코드 확인 | 1 | W2 |
| F05 keep-alive 운영요약·명단 낡음 | 코드 확인 | 4 | W6 |
| F06 예약/취소 전파 안 됨 | 코드 확인 | 2 | W3(취소 API)·W4(전파) |
| F07 팔로우 반영 | **결정 필요** | 4 | W7 — '팔로우 매장만' 필터는 오너 지시로 UI 제거됨(도달 불가 코드). 복원 여부 = 오너 결정 |
| F08 예약 조회 오류가 빈 목록으로 위장 | 코드 확인 | 4 | W3 |
| F09 예약·알림 → 정확한 상세 | 코드 확인 | 4 | W5 |
| F10 설치 배너가 상세를 가림 | 코드 확인 | 1 | W5 |
| F11 늦은 일정 응답이 최신을 덮음 | 코드 확인 | 2 | W4 |

**이미 해결로 확인된 것**(재작업 안 함): 순위 저장 → 이용권/보상 부작용 없음(라이브 `save_venue_rankings` 본문·트리거 0개·`venue_rankings` 직접 쓰기 회수 20260905i 로 실측). 업주 예약 조회 실패 UI(`MyPostersTab` `resErr`)는 이미 정상.

## 2-1. F01~F11 구현 결과(병합 5c2c4b2)

| 항목 | 상태 | 무엇이 달라졌나 | 근거 커밋 |
|---|---|---|---|
| F01 게임 번호 유실 | 실행 검증(단위) | 포스터 '장부 ▼' 목록이 (날짜, 게임)을 넘기고 행에 메인/사이드N 칩. 장부·클락·순위 이동 대상이 그 게임으로 유지 | 1b5e674·d9ec525 |
| F01-b 새 장부가 남의 장부에 착지 | 실행 검증(단위) | 그날 메인 칸이 다른 포스터 장부면 덮지 않고 다음 빈 사이드 칸으로 옮기고 이유를 알린다 | d9ec525 |
| F02 성적 완료 판정 | 실행 검증(단위) | `src/lib/rankingGame.ts` 판정 헬퍼. 같은 날 A만 저장하면 B는 '미입력'이고 B 버튼이 B 게임 입력 화면으로 간다. 판정 불가는 '완료'로 찍지 않는다 | d9ec525 |
| F03 방문 판정 | 실행 검증(단위) | 게임관리 명단이 서버 정본 RPC(계정+매장+그 일정 날짜 KST)를 쓴다. 로컬 오늘 체크인·이름 OR 매칭 제거(동명이인 오판 소멸) | d934168 |
| F04 CRM 금액 | 실행 검증(단위) | `customerLedgerTotals` 로 장부 정본(`buyinFinance`) 재사용. 카드단가·할인·수납 스냅샷 반영, 실수납/미수/이용권/지원을 분리 표기. 원장 무변경 | 0a0ab3d |
| F05 keep-alive·응답 순서 | 실행 검증(단위) | 게임관리가 보일 때만 구독하고 복귀 시 재검증. 펼친 명단이 예약 수와 함께 갱신. 날짜 연타 시 늦은 응답이 다른 날짜 명단을 덮지 않음 | d934168 |
| F06 예약/취소 전파 | 실행 검증(단위) | `resVersion` 신호 하나로 홈 '오늘 예약'·카드 예약 수·캘린더가 따라온다. 새 store·버스 없음 | 512bcdd |
| F06-b 거짓 취소 성공 | 실행 검증(단위) | 세션 없음·0행 삭제를 성공으로 반환하던 것을 throw 로. 화면은 실제 성공에만 바뀐다 | 2f0868e |
| F07 팔로우 | 부분(결정 대기) | 팔로워 수 낙관 갱신·실패 원복, 늦은 GET 이 토글을 덮지 않음, 로그아웃 반영. **'팔로우 매장만' 필터는 오너 지시로 UI 제거된 상태 — 복원 여부 결정 필요** | f52b6da |
| F08 조회 오류 위장 | 실행 검증(단위) | 예약 4개 조회가 실패를 throw. 소비처는 미조회/빈 결과/실패를 구분하고 **직전 성공 데이터를 지우지 않는다** | 2f0868e |
| F09 딥링크 | 실행 검증(단위) | 알림·예약·캘린더가 정확한 id 로 상세를 연다. 목록에 없으면 권한을 지키는 단건 조회, 그래도 없으면 '확인 불가'. 닫으면 원래 화면으로 복귀 | ae77253 |
| F10 설치 배너 | 실행 검증(단위) | 전면 오버레이가 열려 있으면 마운트하지 않는다(z-index 인플레 없음). 설치 기능·닫기 보존 | ae77253 |
| F11 응답 역전 | 실행 검증(단위) | 요청 번호 가드로 늦은 구값이 최신을 덮지 않는다. 삭제 유예 중 재조회가 지운 행을 되살리지 않는다 | 512bcdd |

> '실행 검증(단위)' = tsc·eslint·vitest·프로덕션 빌드·Playwright 회귀까지 통과. **브라우저에서 실제 업무를 저장하며 재현한 것은 아니다**(운영 DB 쓰기 금지·격리 환경 없음).

## 3. 결정 필요 / 승인 대기
- 운영 반영 승인: 위 j·k·l·m·n + 클라 배포(순서 §0).
- BLOCKED #17~19(등수 점수→활동 등급, 클락 TV 프라이즈 표시, 지급된 순위 유래 12점) — 현행 유지 중.

### 이번 작업에서 새로 올라온 결정 사항
1. **'팔로우 매장만' 필터(F07)** — 2026-08-27 오너 지시(f2e1d0b)로 토글 UI 를 지웠고, 그 결과 `followedOnly`·`matchFollow`·전용 빈 상태가 도달 불가 코드가 됐다. 기능 보존 원칙에 따라 **지우지 않았다**. (A) 그대로 둔다 (B) 검색 시트의 조건 칩으로 되살린다(원래 문제였던 폭 겹침이 없다) (C) 걷어낸다. 권장 (A) 또는 (B).
2. **'노쇼' 확정 표기(F03 부수)** — 일정 상세 업주 명단이 체크인 행이 없다는 이유로 종료 후 '노쇼'로 단정한다. 체크인을 운영하지 않는 매장은 전원 노쇼로 보인다. 문구를 '방문 확인 안 됨'으로 낮출지 결정 필요. 지금은 손대지 않았다.
3. **새 장부 착지 방식(F01-b)** — 그날 메인 칸이 다른 포스터의 장부로 차 있을 때 '빈 사이드 칸으로 이동 + 안내'를 택했다. 확인 창을 띄우는 방식을 원하면 바꾼다.
4. **메인 게임의 순위 event 이름** — 진입 경로에 따라 `''` 과 장부 제목 두 갈래가 공존한다. 판정은 둘 다 인정하도록 했고 과거 기록을 소급 통일하지 않았다(임의 확정 금지 원칙).
5. **매장 대시보드 '순위 미입력' 카드** — 게임관리 버튼은 그 게임으로 가지만 이 카드는 여전히 순위 섹션 일반 진입이다(App 계약 변경이 필요해 범위 밖).

## 3-1. 운영 데이터에서 확인된 것(코드 아님 — 오너 판단 필요)

읽기 전용 조회로만 확인했다. **아무것도 지우거나 고치지 않았다.**

1. **같은 회차가 두 번 등록돼 있다** — `로티 단독 1000만 GTD` 2026-08-28 이 두 행이다. 날짜·시작 시각(17:00)·매장·참가비·프라이즈풀·구조가 전부 같고, 등록 계정과 등록 시각만 다르다(8/26 등록분에는 주소·사이드이벤트가 있고, 8/27 등록분에는 없다). 서로 다른 회차가 아니라 **중복 등록**으로 보인다. 문서 지침대로 단정해 지우지 않았다. 남길 행을 정해 주시면 그때 정리한다.
   - 유지 후보 A `ccad91f4-d748-44e2-a0ae-62bb63cf113a`(매장 계정 등록, 주소·사이드이벤트 있음, 조회 11)
   - 유지 후보 B `71a2cf66-7e87-490a-bd97-79d49df45d4e`(다른 계정 등록, 조회 71)
2. **홈 고정 배너의 지난 날짜** — 1번 배너 문구가 `8/27(목) 17:00`인데 게재 기간이 비어 있어(무기한) 오늘도 그대로 뜬다. 링크는 정상이다(로티아레나 매장 페이지, 승인됨). **자동 만료시키지 않았다.** 문구를 고치거나 게재 종료일을 넣는 것은 게시 정책이라 결정이 필요하다.
   - 다만 코드 결함 하나는 고쳤다: 관리자가 배너를 **전부 숨겨도** 코드에 박힌 기본 배너 2장이 되살아나 '내렸는데 홈에 그대로'가 되던 폴백. 이제 기본 배너는 **한 줄도 등록되지 않았을 때만** 쓴다.

## 3-2. 병합 diff 적대적 리뷰(2026-09-06) — 확정 7건 중 6건 수정 완료

6개 차원(병합 정합성·기능 정확성·마이그레이션·배포 호환·규약·UI) 리뷰 + 발견당 2렌즈 반박. 발견 22건 → **확정 7 · 반박 15**.

| 심각도 | 무엇 | 상태 |
|---|---|---|
| 높음 | E2E 쓰기 차단 가드가 세션 발급까지 막아 **UI 로그인 스펙 4건을 죽였다**(CI 게이트 파손) | 수정 — 토큰 발급·갱신·로그아웃만 통과, 계정 생성·비밀번호 변경·복구는 계속 차단 |
| 중간 | 라이브월 삭제 버튼의 `hit` 44px 확장이 **본문 첫 줄을 덮어**, 본문을 누르면 확인 없이 삭제(되돌리기 없음) | 수정 — 실제 박스 + 음수 마진. 행 높이·스켈레톤 계약 유지 |
| 중간 | 숨김(blinded) 글의 **제목·작성자**가 공유 링크로 여전히 노출(본문·댓글만 가려짐), 조회수도 증가 | 수정 — 제목 블록·작성자 헤더 미렌더, 숨김 글 조회수 미집계 |
| 중간 | 조회수 dedupe 키가 **클라이언트가 고르는 헤더**(x-forwarded-for)라 스크립트로 HOT 조작 가능 | 수정 — 엣지가 채우는 cf-connecting-ip, 신뢰 키 없으면 집계 스킵. **적용 전 헤더 실측 절차를 파일에 명시** |
| 중간 | '내 정보' 헤더가 조회 실패를 **'방문 0회'로 단정**(같은 화면 프로필 탭은 오류 카드) | 수정 — 성공 이력이 없으면 '—'. 직전 성공값은 보존 |
| 중간 | 상점 구매 버튼이 잔액 미도착·실패 시 **'N점 소장' 라벨 그대로 비활성**(이유·재시도 없음) | 수정 — `buyLabel` 이 '잔액 확인 중'을 적는다. 테스트 4건 |
| 낮음 | 위 '방문 0회'와 같은 건(다른 차원에서 중복 발견) | 위와 함께 수정 |

**반박 15건**은 대부분 '마이그레이션 j·l·m·n 미적용 상태를 결함으로 본 것'이다. 배포 순서(§0)가 이미 그 구간을 없앤다 — 마이그레이션을 먼저 적용하고 클라이언트를 배포한다.

## 4. 테스트 실행 기록
| 시각 | 명령 | 대상 | 결과 |
|---|---|---|---|
| 22:33 | `npx tsc -b` | 병합 트리 d2d1f92 | exit 0 |
| 22:33 | `npx eslint .` | 〃 | exit 0 · 0 error · 26 warning(기존) |
| 22:33 | `npx vitest run` | 〃 | 54 files · **508 passed** (5.1s) |
| 22:52 | `npx playwright test e2e/write-guard.spec.ts` | 가드 계약 + 브라우저 실증 | **4 passed** — 페이지가 낸 `increment_post_view` POST 는 차단, GET 은 통과 |
| 03:09 | `npx tsc -b` · `npx eslint .` · `npx vitest run` | 리뷰 수정 후 | 0 error · 0 error(26 warning 기존) · **580 passed** |
| 03:12 | `npm run test:e2e` | 리뷰 수정 후 · 쓰기 차단 가드 | main **86 passed · 33 skipped** · boot 2 passed · exit 0 |
| 22:41 | `npm run test:e2e` (build → preview 4173, 쓰기 차단 가드) | da97aab · 운영 프로젝트 읽기 전용 | main **85 passed · 33 skipped**(로그인 스펙 = 미검증) · boot 2 passed · exit 0 |
