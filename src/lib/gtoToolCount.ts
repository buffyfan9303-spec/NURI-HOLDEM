// src/lib/gtoToolCount.ts — 홈 첫 줄이 말하는 "무료 GTO 도구 N개" 의 **N**.
//
// ── 왜 숫자를 여기 따로 두나 ─────────────────────────────────────────────────
// 진짜 목록은 `src/components/features/ToolsPanel.tsx` 에 있다. 그런데 그 모듈은 **lazy 청크**다
// (GTO 탭을 열 때만 내려온다). 홈이 개수를 얻겠다고 그걸 import 하면 GTO 도구 전체가 홈 첫 화면
// 번들로 딸려 들어온다 — 지금 첫 화면 임계 경로 여유가 2% 뿐이라 그 순간 예산이 터진다.
//
// 그래서 숫자만 여기 두고, **거짓말이 되지 않도록 계약으로 묶는다**:
//   `src/lib/gtoToolCount.contract.test.ts` 가 ToolsPanel.tsx 원문을 읽어
//   (TOOLS 항목 수 − GTO 탭에서 숨기는 것) 을 세고 이 값과 같은지 본다.
//   도구를 더하거나 빼면 그 테스트가 먼저 빨개진다.
//
// ⚠ 화면에 적는 수치는 **사실이어야 한다**(§6-1). 이 값을 손으로 고치지 말고,
//   도구를 바꿨으면 계약 테스트가 알려주는 숫자로 맞춰라.
// 🔴 G6(2026-09-20) — 22 → **21**. 2026-09-18 에 '딜 계산기'(`deal`)가 ICM 계산기로 병합되며
//   `HIDDEN_SET` 에 들어갔는데 이 숫자는 그대로였다. 계약 테스트가 `'drill'` 한 단어만 세고 있어
//   **거짓이 초록으로 통과**했다(그 파싱도 같이 고쳤다). 홈 카피가 사실이 되게 21로 내린다.
// 2026-09-23 — 21 → **22**. '스타팅 핸드 순위'(startrank, 규칙 · 대회) 추가.
export const GTO_TOOL_COUNT = 22;
